import { createSlice } from "@reduxjs/toolkit";
const HomeSlice = createSlice({
    name: "home",
    initialState: {
        productlist:[
            
        ]

    },
    reducer: {

    }
})
export default HomeSlice.reducer